■sqlインジェクション
s2daoで.sqlファイルのプレースホルダ(/*names */)をパラメータに置き換えたとき、両端に''が付いた。

sqlはどうやらパラメータに、常に''を付けて「文字列」として解釈している。
というのも、文字列化せずSQL文の一部としてしまうと、悪意あるSQLがパラメータとして渡され、
実行されてしまう可能性があるためである。

このようなSQLを使って不正にデータベースを操作することを目的としている攻撃を「SQLインジェクション」と呼ぶ。
